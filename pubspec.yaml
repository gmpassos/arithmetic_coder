name: arithmetic_coder
description: Adaptive arithmetic coding for efficient lossless compression of bytes in pure Dart.
version: 1.0.0
repository: https://github.com/gmpassos/arithmetic_coder

environment:
  sdk: ^3.10.0

dependencies:
  collection: ^1.19.1
  dependency_validator: ^5.0.5

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.0
